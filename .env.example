# Database Configuration
DATABASE_URL=postgresql://user:password@localhost:5432/cafe_connect

# Server Configuration
PORT=5000
NODE_ENV=development

# Session Configuration (if applicable in future)
SESSION_SECRET=your_super_secret_session_key
